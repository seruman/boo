tell application id "{{.BundleID}}" to send key "{{escape .Key}}"{{if .Action}} action {{.Action}}{{end}}{{if .Modifiers}} modifiers "{{escape .Modifiers}}"{{end}} to terminal id "{{escape .TerminalID}}"
